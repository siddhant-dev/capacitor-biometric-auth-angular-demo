import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'capacitor.biometric.angular.demo',
  appName: 'Biometry Angular',
  webDir: 'www/browser',
};

export default config;
